#include "main.h"

/**
* _abs - checks if a number is positive or negative or zero
*
* Parameters: Integer
*
* @n: int
*
* Return: (-n) if it is negative (n) otherwise
*/

int _abs(int n)
{
if (n < 0)
{
return (-n);
}
else
{
return (n);
}
}
