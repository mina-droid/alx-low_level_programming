#include "main.h"

/**
 *is_palindrome - encodes a string using rot13
 * @s: input string.
 *
 * Return: the pointer to dest.
 */

int is_palindrome(char *s)

{
int len = 
if (*s == '')
{
return (1);
}

}
