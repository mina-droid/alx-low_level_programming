#include "main.h"

/**
 * set_string - encodes a string using rot13
 * @s: input string.
 *
 * @to: input string.
 *
 * Return: the pointer to dest.
 */

void set_string(char **s, char *to)
{
*s = to;
}
